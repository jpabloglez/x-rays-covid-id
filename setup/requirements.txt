# Runtime dependencies. This is the file the backend image installs, so every
# package named in INSTALLED_APPS or imported at request time must appear here.
Django==4.2.16
djangorestframework==3.15.2
django-cors-headers==4.4.0
Pillow==10.4.0
